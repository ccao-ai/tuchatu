# 词跃 · TOEFL Junior

一套面向小托福学习者的 50 词在线能力检测。包含四选一词义题、答题进度、键盘操作、即时评分，以及带英文例句的错词复盘。

## 本地运行

需要 Node.js 22.13 或更新版本。

```bash
npm install
npm run dev
```

构建并验证：

```bash
npm test
```

主要页面代码位于 `app/page.tsx`，视觉样式位于 `app/globals.css`。
